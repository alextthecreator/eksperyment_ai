# Copy to .env.local (do not commit) for `vercel dev` — local API testing.
# On production set the same in Vercel → Environment Variables.

APPS_SCRIPT_URL=https://script.google.com/macros/s/XXXX/exec
